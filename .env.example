# ALB base URL (no trailing slash).
# Path-based routing directs to the right backend service:
#   /auth/*      → Identity Service
#   /ballots/*   → Ballot Service
#   /votes/*     → Vote Service
API_URL=https://your-alb-url.example.com
